<%= form_tag("/microposts/#{@micropost.id}/update") do %>
 <form>
  <div class="form-group">
    <label for="micropost">投稿を編集する</label>
    <textarea name="micropost" class="form-control"><%= @micropost.content %></textarea>
  </div><!--form-group-->
  <button type="submit" class="btn btn-primary">編集</button>
<% end %>