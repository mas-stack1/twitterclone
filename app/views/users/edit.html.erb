<%= form_tag("/users/#{current_user.id}/update") do %>
 <form>
  <div class="form-group">
    <label for="user_edit">ユーザ情報を編集する</label>
    <textarea name="name" class="form-control"><%= current_user.name %></textarea>
  </div><!--form-group-->
  <button type="submit" class="btn btn-primary">保存</button>
  <div class="form-group">
    <label for="user_edit">ユーザ情報を編集する</label>
    <textarea name="email" class="form-control"><%= current_user.email %></textarea>
  </div><!--form-group-->
  <button type="submit" class="btn btn-primary">保存</button>
<% end %>